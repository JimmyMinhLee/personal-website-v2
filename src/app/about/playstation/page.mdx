
import MdxLayout from '@/components/layout/MdxLayout'

# Playstation 

## Overview 

During my sophomore year, I worked remotely for Playstation Now in Aliso Viejo. This was during the onset of the pandemic. 
My team was responsible for building out the business intelligence infrastructure in the system.

import { AnimateChildren } from "@/components/animation/AnimateChildren";
import { Box, List, ListItem, Badge, Divider, HStack, Heading, Text } from "@chakra-ui/react";

<Box py={4} >
        <Text size="sm" as="b">
          Languages:
        <Badge>Scala</Badge>
        <Badge>Python</Badge>
        </Text>
        <Text size="sm" as="b">
          Technologies:
        <Badge>Hadoop</Badge>
        <Badge>Spark</Badge>
        </Text>
</Box>

        <Divider />

<Text pt={2} variant="section-subtitle"> Schema Translation</Text>

My assigned work was to build out a schema transcription layer on the business now ecosystem, for launch of the PS5. 

export default function MDXPage({ children }) {
        return <MdxLayout>{children}</MdxLayout>
}
